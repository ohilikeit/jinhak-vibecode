# Auto-generated from commands/unregister.md — do not edit.
description = "6 AI 호스트에서 jinhak-harness 슬래시 커맨드 모두 제거"
prompt = """
# /unregister — 슬래시 커맨드 제거

`register` 의 역연산. 6 호스트 user-level dir의 `jinhak/` 서브디렉터리를 비웁니다. CLI 자체(글로벌 바이너리)는 영향받지 않습니다.

## 실행

```bash
npx -y jinhak-harness unregister
```

미리 보기:

```bash
npx -y jinhak-harness unregister --dry-run
```

특정 호스트만:

```bash
npx -y jinhak-harness unregister --host=claude
```

`$ARGUMENTS` 그대로 전달:

```bash
npx -y jinhak-harness unregister $ARGUMENTS
```

## 완전 삭제 (CLI 포함)

```bash
npx -y jinhak-harness unregister          # 호스트 슬래시 커맨드 제거
npm uninstall -g jinhak-harness    # CLI 글로벌 제거
rm -rf ~/.harness                  # 설정·메모리 제거 (선택)
```

"""
